apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend.fullname" . }}-test-connection-{{ randAlphaNum 5 | lower }}"
  labels:
{{ include "frontend.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: grpc-test
      image: gcr.io/manage-261115/grpc-test:latest
      command: ["/bin/grpc_health_probe", "-addr={{ include "frontend.fullname" . }}:{{ .Values.service.port }}"]
  restartPolicy: Never