apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis.fullname" . }}-test-connection-{{ randAlphaNum 5 | lower }}"
  labels:
{{ include "redis.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "redis.fullname" . }}-cart:{{ .Values.service.port }}']
  restartPolicy: Never
