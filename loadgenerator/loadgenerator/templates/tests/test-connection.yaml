apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loadgenerator.fullname" . }}-test-connection-{{ randAlphaNum 5 }}"
  labels:
{{ include "loadgenerator.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "loadgenerator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
