apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emailservice.fullname" . }}-test-connection-{{ lower randAlphaNum 5 }}"
  labels:
{{ include "emailservice.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "emailservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
