apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "currencyservice.fullname" . }}-test-connection-{{ randAlphaNum 5 }}"
  labels:
{{ include "currencyservice.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "currencyservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
