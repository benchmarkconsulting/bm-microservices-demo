apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "productcatalogservice.fullname" . }}-test-connection-{{ randAlphaNum 5 }}"
  labels:
{{ include "productcatalogservice.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "productcatalogservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
