apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cartservice.fullname" . }}-test-connection-{{ randAlphaNum 5 }}"
  labels:
{{ include "cartservice.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cartservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never